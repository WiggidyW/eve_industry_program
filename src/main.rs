mod api_data;
mod common;
mod composite;
mod config_data;
mod run_data;
mod static_data;

mod typedef;
use typedef::*;

mod industry_db;

mod config;

mod error;
use error::Error;

mod runtime;

mod api_data_rename;

fn main() {
    println!("Hello, world!");
}
